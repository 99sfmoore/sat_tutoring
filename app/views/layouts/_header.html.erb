
  <div class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class = "container">
      <%= link_to "SAT Home", tutor_path(current_user), class: "navbar-brand" %>
      <ul class="nav navbar-nav navbar-right">
        <% if signed_in? %>
          <li><%= link_to "Site", site_path(@current_user.site) %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Students <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <% current_user.students.each do |s| %>
                <li><%= link_to s.first_name, student_path(s) %></li>
              <% end %>
            </ul>
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Actions <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Plan Lessons", tutor_lesson_plans_path(current_user) %></li>
            <li><%= link_to "Edit Profile", edit_tutor_path(current_user)%></li>
          </ul>
          <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
        <% end %>
      </ul>
    </div>
  </div>