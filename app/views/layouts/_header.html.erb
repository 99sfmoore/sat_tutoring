
  <div class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class = "container">
      <%= link_to image_tag('apple.png'), "http://www.newyorkcares.org", target: "_blank",class: "brand-picture"%>
      <%= link_to image_tag('pencil.png'), root_path, class: "brand-picture"%>
      <%= link_to "SAT Home", root_path, class: "navbar-brand"%>
      <ul class="nav navbar-nav navbar-right">
        <% if signed_in? %>
          <li><%= link_to current_user.site.abbreviation, site_path(current_user.site) %></li>
          <li><%= link_to "Your Group", tutor_path(current_user) %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Your Students<b class="caret"></b></a>
            <ul class="dropdown-menu">
              <% current_user.students.each do |s| %>
                <li><%= link_to s.first_name, student_path(s) %></li>
              <% end %>
            </ul>
          </li>
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Actions <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Plan Lessons", tutor_lesson_plans_path(current_user) %></li>
            <li><%= link_to "Edit Profile", edit_tutor_path(current_user)%></li>
            <li><%= link_to "Send HW emails", hw_email_lesson_plan_path(last_lesson)%></li>
          </ul>
          <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
        <% end %>
      </ul>
    </div>
  </div>