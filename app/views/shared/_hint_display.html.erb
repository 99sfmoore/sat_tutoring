<% if hint.nil? %>
  <%= link_to "Write hint", question_assignment_hw_hints_path(q,assignment) %>
<% else %>
  <%= hint.hint %>
  <div class="small subtle">
    <br>
    <% if ["","-",nil].include?(hint.answer_choice) %>
      general hint,
    <% else %>
      hint for <%= hint.answer_choice %>,
    <% end %>
     written by: <%= hint.tutor.full_name %>
  </div>
<% end %>
